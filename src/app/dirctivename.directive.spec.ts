import { DirctivenameDirective } from './dirctivename.directive';

describe('DirctivenameDirective', () => {
  it('should create an instance', () => {
    const directive = new DirctivenameDirective();
    expect(directive).toBeTruthy();
  });
});
